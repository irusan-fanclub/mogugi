log*.txt
*.exe
node_modules
dist
cmd/dilmeterapi/static
bin

# runtime artifacts
dilmeter_*.log
packet_capture_*.pcapng
packet_log_*.ndjson

# tool local data
.claude/
logs/

# license / keys (must never be committed)
license.key
release-keys.txt
